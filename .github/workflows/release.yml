name: Release
on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  artifacts:
    name: Upload release artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set correct versions
        run: |
          VERSION=${{ github.event.release.tag_name }}
          VERSION=${VERSION:1} # Remove v prefix
          VERSION=${VERSION%.*} # Remove minor version
          sed "s/edge/$VERSION/" -i docker-compose.yml

      - name: Upload artifacts to draft release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          tag_name: ${{ github.event.release.tag_name }}
          files: |
            ./docker-compose.yml
            ./.env.example

  native:
    name: Build Android ${{ matrix.tv && 'TV' || 'phone' }} APK
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        tv: [false, true]
    uses: ./.github/workflows/android-build.yml
    with:
      variant: release
      tv: ${{ matrix.tv }}
      attach-to-release: true
      release-tag: ${{ github.event.release.tag_name }}
    secrets: inherit

  publish:
    name: Publish release
    runs-on: ubuntu-latest
    needs: [artifacts, native]
    # Publish once every asset is attached. `always()` lets this run even when the
    # native job is skipped; we only block on an outright failure.
    if: always() && needs.artifacts.result == 'success' && needs.native.result != 'failure'
    steps:
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit ${{ github.event.release.tag_name }} --draft=false --repo ${{ github.repository }}
