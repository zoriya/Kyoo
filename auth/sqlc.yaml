version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries"
    schema: "sql/migrations"
    gen:
      go:
        package: "dbc"
        out: "dbc"
        overrides:
        - db_type: "timestampz"
          go_type:
            import: "time"
            type: "Time"

