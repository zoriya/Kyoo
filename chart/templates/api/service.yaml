apiVersion: v1
kind: Service
metadata:
{{- if .Values.api.service.annotations }}
  annotations:
  {{- range $key, $value := .Values.api.service.annotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
  name: {{ include "kyoo.api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kyoo.labels" (dict "context" . "component" .Values.api.name "name" .Values.api.name) | nindent 4 }}
    {{- with .Values.api.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: 3567
      targetPort: 3567
      protocol: TCP
      name: main
  selector:
    {{- include "kyoo.selectorLabels" (dict "context" . "name" .Values.api.name) | nindent 4 }}
