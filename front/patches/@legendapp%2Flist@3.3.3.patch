diff --git a/react-native.js b/react-native.js
index 229f09a8230ab24079d471f65d28d36908f0f2ea..24a089985c58132c580851921ee33d9b72fcd46e 100644
--- a/react-native.js
+++ b/react-native.js
@@ -3958,10 +3958,13 @@ function scheduleRenderRangeProjectionSettle(ctx) {
 }
 function findCurrentStickyIndex(stickyArray, scroll, state) {
   const positions = state.positions;
+  // Clamp scroll to 0 minimum since negative values (from padding offset) should
+  // still activate the first sticky header at position 0
+  const effectiveScroll = Math.max(0, scroll);
   for (let i = stickyArray.length - 1; i >= 0; i--) {
     const stickyIndex = stickyArray[i];
     const stickyPos = positions[stickyIndex];
-    if (stickyPos !== void 0 && scroll >= stickyPos) {
+    if (stickyPos !== void 0 && effectiveScroll >= stickyPos) {
       return i;
     }
   }
@@ -5096,7 +5099,7 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
     () => getStickyPushLimit(ctx.state, itemIndex, itemKey),
     [ctx.state, itemIndex, itemKey, _totalSize]
   );
-  const transform = React2__namespace.useMemo(() => {
+  const stickyOffset = React2__namespace.useMemo(() => {
     var _a3;
     if (animatedScrollY) {
       const stickyConfigOffset = (_a3 = stickyHeaderConfig == null ? void 0 : stickyHeaderConfig.offset) != null ? _a3 : 0;
@@ -5104,13 +5107,13 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
       let nextStickyPosition;
       if (pushLimit !== void 0) {
         if (pushLimit <= position) {
-          nextStickyPosition = pushLimit;
+          nextStickyPosition = pushLimit - position;
         } else {
           nextStickyPosition = animatedScrollY.interpolate({
             extrapolateLeft: "clamp",
             extrapolateRight: "clamp",
             inputRange: [stickyStart, stickyStart + (pushLimit - position)],
-            outputRange: [position, pushLimit]
+            outputRange: [0, pushLimit - position]
           });
         }
       } else {
@@ -5118,7 +5121,7 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
           extrapolateLeft: "clamp",
           extrapolateRight: "extend",
           inputRange: [stickyStart, stickyStart + 5e3],
-          outputRange: [position, position + 5e3]
+          outputRange: [0, 5e3]
         });
       }
       return horizontal ? [{ translateX: nextStickyPosition }] : [{ translateY: nextStickyPosition }];
@@ -5133,8 +5136,13 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
     stickyHeaderConfig == null ? void 0 : stickyHeaderConfig.offset
   ]);
   const viewStyle = React2__namespace.useMemo(
-    () => [style, { zIndex: itemIndex + 1e3 }, { transform }],
-    [style, itemIndex, transform]
+    () => [
+      style,
+      { zIndex: itemIndex + 1e3 },
+      horizontal ? { left: position } : { top: position },
+      { transform: stickyOffset }
+    ],
+    [style, itemIndex, stickyOffset, horizontal, position]
   );
   const renderStickyHeaderBackdrop = React2__namespace.useMemo(() => {
     if (!(stickyHeaderConfig == null ? void 0 : stickyHeaderConfig.backdropComponent)) {
diff --git a/react-native.mjs b/react-native.mjs
index c2e0f3823f524e31e2105a5fbda76be78c926226..3a475daf7212054a947f76add8b8fe3e8a51020a 100644
--- a/react-native.mjs
+++ b/react-native.mjs
@@ -3937,10 +3937,13 @@ function scheduleRenderRangeProjectionSettle(ctx) {
 }
 function findCurrentStickyIndex(stickyArray, scroll, state) {
   const positions = state.positions;
+  // Clamp scroll to 0 minimum since negative values (from padding offset) should
+  // still activate the first sticky header at position 0
+  const effectiveScroll = Math.max(0, scroll);
   for (let i = stickyArray.length - 1; i >= 0; i--) {
     const stickyIndex = stickyArray[i];
     const stickyPos = positions[stickyIndex];
-    if (stickyPos !== void 0 && scroll >= stickyPos) {
+    if (stickyPos !== void 0 && effectiveScroll >= stickyPos) {
       return i;
     }
   }
@@ -5075,7 +5078,7 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
     () => getStickyPushLimit(ctx.state, itemIndex, itemKey),
     [ctx.state, itemIndex, itemKey, _totalSize]
   );
-  const transform = React2.useMemo(() => {
+  const stickyOffset = React2.useMemo(() => {
     var _a3;
     if (animatedScrollY) {
       const stickyConfigOffset = (_a3 = stickyHeaderConfig == null ? void 0 : stickyHeaderConfig.offset) != null ? _a3 : 0;
@@ -5083,13 +5086,13 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
       let nextStickyPosition;
       if (pushLimit !== void 0) {
         if (pushLimit <= position) {
-          nextStickyPosition = pushLimit;
+          nextStickyPosition = pushLimit - position;
         } else {
           nextStickyPosition = animatedScrollY.interpolate({
             extrapolateLeft: "clamp",
             extrapolateRight: "clamp",
             inputRange: [stickyStart, stickyStart + (pushLimit - position)],
-            outputRange: [position, pushLimit]
+            outputRange: [0, pushLimit - position]
           });
         }
       } else {
@@ -5097,7 +5100,7 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
           extrapolateLeft: "clamp",
           extrapolateRight: "extend",
           inputRange: [stickyStart, stickyStart + 5e3],
-          outputRange: [position, position + 5e3]
+          outputRange: [0, 5e3]
         });
       }
       return horizontal ? [{ translateX: nextStickyPosition }] : [{ translateY: nextStickyPosition }];
@@ -5112,8 +5115,13 @@ var PositionViewSticky = typedMemo(function PositionViewSticky2({
     stickyHeaderConfig == null ? void 0 : stickyHeaderConfig.offset
   ]);
   const viewStyle = React2.useMemo(
-    () => [style, { zIndex: itemIndex + 1e3 }, { transform }],
-    [style, itemIndex, transform]
+    () => [
+      style,
+      { zIndex: itemIndex + 1e3 },
+      horizontal ? { left: position } : { top: position },
+      { transform: stickyOffset }
+    ],
+    [style, itemIndex, stickyOffset, horizontal, position]
   );
   const renderStickyHeaderBackdrop = React2.useMemo(() => {
     if (!(stickyHeaderConfig == null ? void 0 : stickyHeaderConfig.backdropComponent)) {
diff --git a/react-native.web.js b/react-native.web.js
index 544cca82136ab14cbeec0a01499f554bd4eca75f..4987da52de388646bb66dadd4e3a73a9d10c9631 100644
--- a/react-native.web.js
+++ b/react-native.web.js
@@ -3980,10 +3980,13 @@ function scheduleRenderRangeProjectionSettle(ctx) {
 }
 function findCurrentStickyIndex(stickyArray, scroll, state) {
   const positions = state.positions;
+  // Clamp scroll to 0 minimum since negative values (from padding offset) should
+  // still activate the first sticky header at position 0
+  const effectiveScroll = Math.max(0, scroll);
   for (let i = stickyArray.length - 1; i >= 0; i--) {
     const stickyIndex = stickyArray[i];
     const stickyPos = positions[stickyIndex];
-    if (stickyPos !== void 0 && scroll >= stickyPos) {
+    if (stickyPos !== void 0 && effectiveScroll >= stickyPos) {
       return i;
     }
   }
diff --git a/react-native.web.mjs b/react-native.web.mjs
index 6b2d9011f6c5e85664eca195de538617fe7d9dee..364a477d308a7c62c35a9fe6371d59bbb33a3ba0 100644
--- a/react-native.web.mjs
+++ b/react-native.web.mjs
@@ -3959,10 +3959,13 @@ function scheduleRenderRangeProjectionSettle(ctx) {
 }
 function findCurrentStickyIndex(stickyArray, scroll, state) {
   const positions = state.positions;
+  // Clamp scroll to 0 minimum since negative values (from padding offset) should
+  // still activate the first sticky header at position 0
+  const effectiveScroll = Math.max(0, scroll);
   for (let i = stickyArray.length - 1; i >= 0; i--) {
     const stickyIndex = stickyArray[i];
     const stickyPos = positions[stickyIndex];
-    if (stickyPos !== void 0 && scroll >= stickyPos) {
+    if (stickyPos !== void 0 && effectiveScroll >= stickyPos) {
       return i;
     }
   }
diff --git a/react.js b/react.js
index 544cca82136ab14cbeec0a01499f554bd4eca75f..4987da52de388646bb66dadd4e3a73a9d10c9631 100644
--- a/react.js
+++ b/react.js
@@ -3980,10 +3980,13 @@ function scheduleRenderRangeProjectionSettle(ctx) {
 }
 function findCurrentStickyIndex(stickyArray, scroll, state) {
   const positions = state.positions;
+  // Clamp scroll to 0 minimum since negative values (from padding offset) should
+  // still activate the first sticky header at position 0
+  const effectiveScroll = Math.max(0, scroll);
   for (let i = stickyArray.length - 1; i >= 0; i--) {
     const stickyIndex = stickyArray[i];
     const stickyPos = positions[stickyIndex];
-    if (stickyPos !== void 0 && scroll >= stickyPos) {
+    if (stickyPos !== void 0 && effectiveScroll >= stickyPos) {
       return i;
     }
   }
diff --git a/react.mjs b/react.mjs
index 6b2d9011f6c5e85664eca195de538617fe7d9dee..364a477d308a7c62c35a9fe6371d59bbb33a3ba0 100644
--- a/react.mjs
+++ b/react.mjs
@@ -3959,10 +3959,13 @@ function scheduleRenderRangeProjectionSettle(ctx) {
 }
 function findCurrentStickyIndex(stickyArray, scroll, state) {
   const positions = state.positions;
+  // Clamp scroll to 0 minimum since negative values (from padding offset) should
+  // still activate the first sticky header at position 0
+  const effectiveScroll = Math.max(0, scroll);
   for (let i = stickyArray.length - 1; i >= 0; i--) {
     const stickyIndex = stickyArray[i];
     const stickyPos = positions[stickyIndex];
-    if (stickyPos !== void 0 && scroll >= stickyPos) {
+    if (stickyPos !== void 0 && effectiveScroll >= stickyPos) {
       return i;
     }
   }
