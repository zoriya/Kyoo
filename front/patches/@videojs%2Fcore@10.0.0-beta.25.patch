diff --git a/node_modules/@videojs/core/.bun-tag-2ae87bfdc8c60c18 b/.bun-tag-2ae87bfdc8c60c18
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@videojs/core/.bun-tag-6ae2888ce9205340 b/.bun-tag-6ae2888ce9205340
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@videojs/core/.bun-tag-c99bf98327cf098c b/.bun-tag-c99bf98327cf098c
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/default/dom/media/google-cast/google-cast-provider.js b/dist/default/dom/media/google-cast/google-cast-provider.js
index 1ab903161eed3118320a71fb2a4be8c606be87de..21cbafc728be9d02cc9890eccf1821874df47108 100644
--- a/dist/default/dom/media/google-cast/google-cast-provider.js
+++ b/dist/default/dom/media/google-cast/google-cast-provider.js
@@ -110,7 +110,11 @@ var GoogleCastProvider = class {
 		});
 		mediaInfo.streamType = this.#googleCast.streamType === "live" ? chrome.cast.media.StreamType.LIVE : chrome.cast.media.StreamType.BUFFERED;
 		mediaInfo.metadata = new chrome.cast.media.GenericMediaMetadata();
-		mediaInfo.metadata.images = [new chrome.cast.Image(target?.poster ?? "")];
+		const castMeta = mediaInfo.customData ?? {};
+		if (castMeta.title) mediaInfo.metadata.title = castMeta.title;
+		if (castMeta.subtitle) mediaInfo.metadata.subtitle = castMeta.subtitle;
+		const castImage = castMeta.poster ?? target?.poster;
+		if (castImage) mediaInfo.metadata.images = [new chrome.cast.Image(castImage)];
 		if (await isHls(this.#googleCast.src)) {
 			mediaInfo.contentType ||= "application/x-mpegURL";
 			const fmt = await getPlaylistSegmentFormat(this.#googleCast.src) ?? "";
diff --git a/dist/default/dom/media/google-cast/utils.js b/dist/default/dom/media/google-cast/utils.js
index 8ca144e0a61f3f25b7628964a2dff818efc689f3..ef2660883dc0be47e18009773d0922a95ac274dd 100644
--- a/dist/default/dom/media/google-cast/utils.js
+++ b/dist/default/dom/media/google-cast/utils.js
@@ -52,6 +52,24 @@ function getCastContext() {
 function currentSession() {
 	return getCastContext()?.getCurrentSession();
 }
+function castConnectionState() {
+	const ctx = getCastContext();
+	if (!ctx || typeof cast === "undefined") return "disconnected";
+	switch (ctx.getCastState()) {
+		case cast.framework.CastState.CONNECTED: return "connected";
+		case cast.framework.CastState.CONNECTING: return "connecting";
+		default: return "disconnected";
+	}
+}
+function castDevicesAvailable() {
+	const ctx = getCastContext();
+	if (!ctx || typeof cast === "undefined") return false;
+	const state = ctx.getCastState();
+	return !!state && state !== cast.framework.CastState.NO_DEVICES_AVAILABLE;
+}
+function endCastSession(stopCasting = true) {
+	getCastContext()?.endCurrentSession(stopCasting);
+}
 function currentMedia() {
 	return currentSession()?.getSessionObj().media[0] ?? void 0;
 }
@@ -138,6 +156,6 @@ async function getPlaylistSegmentFormat(url) {
 	}
 }
 //#endregion
-export { InvalidStateError, IterableWeakSet, NotFoundError, NotSupportedError, currentMedia, currentSession, editTracksInfo, getCastContext, getMediaStatus, getPlaylistSegmentFormat, isHls, loadCastFramework, onCastApiAvailable, requiresCastFramework, setCastOptions, setPlaybackRate };
+export { InvalidStateError, IterableWeakSet, NotFoundError, NotSupportedError, castConnectionState, castDevicesAvailable, currentMedia, currentSession, editTracksInfo, endCastSession, getCastContext, getMediaStatus, getPlaylistSegmentFormat, isHls, loadCastFramework, onCastApiAvailable, requiresCastFramework, setCastOptions, setPlaybackRate };
 
 //# sourceMappingURL=utils.js.map
\ No newline at end of file
diff --git a/dist/default/dom/media/hls-js/media-tracks.js b/dist/default/dom/media/hls-js/media-tracks.js
index 99ca6c647292de8ee811f6b0595b23744301fa88..fadfd434b71d2053e6565857a9a8db6a5b658b63 100644
--- a/dist/default/dom/media/hls-js/media-tracks.js
+++ b/dist/default/dom/media/hls-js/media-tracks.js
@@ -22,6 +22,8 @@ function HlsJsMediaMediaTracksMixin(BaseClass) {
 			if (!engine) return;
 			engine.on(Hls.Events.MANIFEST_PARSED, this.#onManifestParsed);
 			engine.on(Hls.Events.AUDIO_TRACKS_UPDATED, this.#onAudioTracksUpdated);
+			engine.on(Hls.Events.AUDIO_TRACK_SWITCHING, this.#reflectAudioSelection);
+			engine.on(Hls.Events.AUDIO_TRACK_SWITCHED, this.#reflectAudioSelection);
 			engine.on(Hls.Events.LEVELS_UPDATED, this.#onLevelsUpdated);
 			engine.on(Hls.Events.LEVEL_SWITCHED, this.#onLevelSwitched);
 			engine.once(Hls.Events.DESTROYING, this.#teardown);
@@ -41,14 +43,30 @@ function HlsJsMediaMediaTracksMixin(BaseClass) {
 			}
 		};
 		#onAudioTracksUpdated = (_event, data) => {
+			if (this.#audioTracksMatch(data.audioTracks)) return;
 			this.#removeAudioTracks();
 			for (const hlsAudioTrack of data.audioTracks) {
 				const kind = hlsAudioTrack.default ? "main" : "alternative";
 				const audioTrack = this.addAudioTrack(kind, hlsAudioTrack.name, hlsAudioTrack.lang);
 				audioTrack.id = `${hlsAudioTrack.id}`;
-				audioTrack.enabled = Boolean(hlsAudioTrack.default);
 			}
 		};
+		#reflectAudioSelection = (_event, data) => {
+			const selectedId = `${data.id}`;
+			for (const track of this.audioTracks) track.enabled = track.id === selectedId;
+		};
+		#audioTracksMatch(incoming) {
+			const current = [...this.audioTracks];
+			if (current.length !== incoming.length) return false;
+			return incoming.every((hlsAudioTrack, index) => {
+				const existing = current[index];
+				return (
+					existing?.id === `${hlsAudioTrack.id}` &&
+					existing.label === (hlsAudioTrack.name ?? "") &&
+					existing.language === (hlsAudioTrack.lang ?? "")
+				);
+			});
+		}
 		#switchAudioTrack = () => {
 			const { engine } = this;
 			if (!engine) return;
@@ -79,6 +97,8 @@ function HlsJsMediaMediaTracksMixin(BaseClass) {
 			const { engine } = this;
 			engine?.off(Hls.Events.MANIFEST_PARSED, this.#onManifestParsed);
 			engine?.off(Hls.Events.AUDIO_TRACKS_UPDATED, this.#onAudioTracksUpdated);
+			engine?.off(Hls.Events.AUDIO_TRACK_SWITCHING, this.#reflectAudioSelection);
+			engine?.off(Hls.Events.AUDIO_TRACK_SWITCHED, this.#reflectAudioSelection);
 			engine?.off(Hls.Events.LEVELS_UPDATED, this.#onLevelsUpdated);
 			engine?.off(Hls.Events.LEVEL_SWITCHED, this.#onLevelSwitched);
 			engine?.off(Hls.Events.DESTROYING, this.#teardown);
diff --git a/dist/default/dom/store/features/remote-playback.js b/dist/default/dom/store/features/remote-playback.js
index 9a00dbf2858199afe8a66c8f3796ba78efdfc7bc..16737d05303a2806575ba6ea0984d84bb8266af7 100644
--- a/dist/default/dom/store/features/remote-playback.js
+++ b/dist/default/dom/store/features/remote-playback.js
@@ -1,7 +1,9 @@
 import { isMediaRemotePlaybackCapable } from "../../../media/predicate.js";
 import { definePlayerFeature } from "../../feature.js";
-import { isRemotePlaybackConnected, requestRemotePlayback } from "../../presentation/remote-playback.js";
+import { isRemotePlaybackConnected, isRemotePlaybackConnecting, requestRemotePlayback } from "../../presentation/remote-playback.js";
 import { exitFullscreen, isFullscreen } from "../../presentation/fullscreen.js";
+import { castConnectionState, castDevicesAvailable, endCastSession, getCastContext, requiresCastFramework } from "../../media/google-cast/utils.js";
+import { castFramework, ensureCastFramework } from "../../media/google-cast/registry.js";
 import { isWebKitAirPlayCapable, listen } from "@videojs/utils/dom";
 //#region src/dom/store/features/remote-playback.ts
 const remotePlaybackFeature = definePlayerFeature({
@@ -11,15 +13,21 @@ const remotePlaybackFeature = definePlayerFeature({
 		remotePlaybackAvailability: "unsupported",
 		async toggleRemotePlayback() {
 			const { media, container } = target();
-			if (isRemotePlaybackConnected(media)) return requestRemotePlayback(media);
+			const capable = Boolean(isMediaRemotePlaybackCapable(media));
+			const connected = isRemotePlaybackConnected(media) || isRemotePlaybackConnecting(media) || castConnectionState() !== "disconnected";
+			if (connected) {
+				if (capable) return requestRemotePlayback(media);
+				endCastSession();
+				return;
+			}
+			if (!capable) return;
 			if (isFullscreen(container, media)) await exitFullscreen(media);
 			return await requestRemotePlayback(media);
 		}
 	}),
 	attach({ target, signal, set }) {
 		const { media } = target;
-		if (!isMediaRemotePlaybackCapable(media)) return;
-		if (isWebKitAirPlayCapable(media)) {
+		if (media && isWebKitAirPlayCapable(media)) {
 			const syncConnection = () => {
 				set({ remotePlaybackState: media.webkitCurrentPlaybackTargetIsWireless ? "connected" : "disconnected" });
 			};
@@ -32,6 +40,29 @@ const remotePlaybackFeature = definePlayerFeature({
 			syncConnection();
 			return;
 		}
+		if (requiresCastFramework()) {
+			const sync = () => {
+				set({
+					remotePlaybackState: castConnectionState(),
+					remotePlaybackAvailability: castDevicesAvailable() ? "available" : "unavailable"
+				});
+			};
+			ensureCastFramework().then(() => {
+				if (signal.aborted) return;
+				const ctx = getCastContext();
+				if (!ctx || !castFramework) return;
+				const { CAST_STATE_CHANGED, SESSION_STATE_CHANGED } = castFramework.CastContextEventType;
+				ctx.addEventListener(CAST_STATE_CHANGED, sync);
+				ctx.addEventListener(SESSION_STATE_CHANGED, sync);
+				signal.addEventListener("abort", () => {
+					ctx.removeEventListener(CAST_STATE_CHANGED, sync);
+					ctx.removeEventListener(SESSION_STATE_CHANGED, sync);
+				});
+				sync();
+			}, () => set({ remotePlaybackAvailability: "unsupported" }));
+			return;
+		}
+		if (!isMediaRemotePlaybackCapable(media)) return;
 		const syncState = () => set({ remotePlaybackState: media.remote.state });
 		syncState();
 		listen(media.remote, "connect", syncState, { signal });
diff --git a/dist/dev/dom/media/google-cast/google-cast-provider.js b/dist/dev/dom/media/google-cast/google-cast-provider.js
index 1ab903161eed3118320a71fb2a4be8c606be87de..21cbafc728be9d02cc9890eccf1821874df47108 100644
--- a/dist/dev/dom/media/google-cast/google-cast-provider.js
+++ b/dist/dev/dom/media/google-cast/google-cast-provider.js
@@ -110,7 +110,11 @@ var GoogleCastProvider = class {
 		});
 		mediaInfo.streamType = this.#googleCast.streamType === "live" ? chrome.cast.media.StreamType.LIVE : chrome.cast.media.StreamType.BUFFERED;
 		mediaInfo.metadata = new chrome.cast.media.GenericMediaMetadata();
-		mediaInfo.metadata.images = [new chrome.cast.Image(target?.poster ?? "")];
+		const castMeta = mediaInfo.customData ?? {};
+		if (castMeta.title) mediaInfo.metadata.title = castMeta.title;
+		if (castMeta.subtitle) mediaInfo.metadata.subtitle = castMeta.subtitle;
+		const castImage = castMeta.poster ?? target?.poster;
+		if (castImage) mediaInfo.metadata.images = [new chrome.cast.Image(castImage)];
 		if (await isHls(this.#googleCast.src)) {
 			mediaInfo.contentType ||= "application/x-mpegURL";
 			const fmt = await getPlaylistSegmentFormat(this.#googleCast.src) ?? "";
diff --git a/dist/dev/dom/media/google-cast/utils.js b/dist/dev/dom/media/google-cast/utils.js
index 8ca144e0a61f3f25b7628964a2dff818efc689f3..ef2660883dc0be47e18009773d0922a95ac274dd 100644
--- a/dist/dev/dom/media/google-cast/utils.js
+++ b/dist/dev/dom/media/google-cast/utils.js
@@ -52,6 +52,24 @@ function getCastContext() {
 function currentSession() {
 	return getCastContext()?.getCurrentSession();
 }
+function castConnectionState() {
+	const ctx = getCastContext();
+	if (!ctx || typeof cast === "undefined") return "disconnected";
+	switch (ctx.getCastState()) {
+		case cast.framework.CastState.CONNECTED: return "connected";
+		case cast.framework.CastState.CONNECTING: return "connecting";
+		default: return "disconnected";
+	}
+}
+function castDevicesAvailable() {
+	const ctx = getCastContext();
+	if (!ctx || typeof cast === "undefined") return false;
+	const state = ctx.getCastState();
+	return !!state && state !== cast.framework.CastState.NO_DEVICES_AVAILABLE;
+}
+function endCastSession(stopCasting = true) {
+	getCastContext()?.endCurrentSession(stopCasting);
+}
 function currentMedia() {
 	return currentSession()?.getSessionObj().media[0] ?? void 0;
 }
@@ -138,6 +156,6 @@ async function getPlaylistSegmentFormat(url) {
 	}
 }
 //#endregion
-export { InvalidStateError, IterableWeakSet, NotFoundError, NotSupportedError, currentMedia, currentSession, editTracksInfo, getCastContext, getMediaStatus, getPlaylistSegmentFormat, isHls, loadCastFramework, onCastApiAvailable, requiresCastFramework, setCastOptions, setPlaybackRate };
+export { InvalidStateError, IterableWeakSet, NotFoundError, NotSupportedError, castConnectionState, castDevicesAvailable, currentMedia, currentSession, editTracksInfo, endCastSession, getCastContext, getMediaStatus, getPlaylistSegmentFormat, isHls, loadCastFramework, onCastApiAvailable, requiresCastFramework, setCastOptions, setPlaybackRate };
 
 //# sourceMappingURL=utils.js.map
\ No newline at end of file
diff --git a/dist/dev/dom/media/hls-js/media-tracks.js b/dist/dev/dom/media/hls-js/media-tracks.js
index 99ca6c647292de8ee811f6b0595b23744301fa88..fadfd434b71d2053e6565857a9a8db6a5b658b63 100644
--- a/dist/dev/dom/media/hls-js/media-tracks.js
+++ b/dist/dev/dom/media/hls-js/media-tracks.js
@@ -22,6 +22,8 @@ function HlsJsMediaMediaTracksMixin(BaseClass) {
 			if (!engine) return;
 			engine.on(Hls.Events.MANIFEST_PARSED, this.#onManifestParsed);
 			engine.on(Hls.Events.AUDIO_TRACKS_UPDATED, this.#onAudioTracksUpdated);
+			engine.on(Hls.Events.AUDIO_TRACK_SWITCHING, this.#reflectAudioSelection);
+			engine.on(Hls.Events.AUDIO_TRACK_SWITCHED, this.#reflectAudioSelection);
 			engine.on(Hls.Events.LEVELS_UPDATED, this.#onLevelsUpdated);
 			engine.on(Hls.Events.LEVEL_SWITCHED, this.#onLevelSwitched);
 			engine.once(Hls.Events.DESTROYING, this.#teardown);
@@ -41,14 +43,30 @@ function HlsJsMediaMediaTracksMixin(BaseClass) {
 			}
 		};
 		#onAudioTracksUpdated = (_event, data) => {
+			if (this.#audioTracksMatch(data.audioTracks)) return;
 			this.#removeAudioTracks();
 			for (const hlsAudioTrack of data.audioTracks) {
 				const kind = hlsAudioTrack.default ? "main" : "alternative";
 				const audioTrack = this.addAudioTrack(kind, hlsAudioTrack.name, hlsAudioTrack.lang);
 				audioTrack.id = `${hlsAudioTrack.id}`;
-				audioTrack.enabled = Boolean(hlsAudioTrack.default);
 			}
 		};
+		#reflectAudioSelection = (_event, data) => {
+			const selectedId = `${data.id}`;
+			for (const track of this.audioTracks) track.enabled = track.id === selectedId;
+		};
+		#audioTracksMatch(incoming) {
+			const current = [...this.audioTracks];
+			if (current.length !== incoming.length) return false;
+			return incoming.every((hlsAudioTrack, index) => {
+				const existing = current[index];
+				return (
+					existing?.id === `${hlsAudioTrack.id}` &&
+					existing.label === (hlsAudioTrack.name ?? "") &&
+					existing.language === (hlsAudioTrack.lang ?? "")
+				);
+			});
+		}
 		#switchAudioTrack = () => {
 			const { engine } = this;
 			if (!engine) return;
@@ -79,6 +97,8 @@ function HlsJsMediaMediaTracksMixin(BaseClass) {
 			const { engine } = this;
 			engine?.off(Hls.Events.MANIFEST_PARSED, this.#onManifestParsed);
 			engine?.off(Hls.Events.AUDIO_TRACKS_UPDATED, this.#onAudioTracksUpdated);
+			engine?.off(Hls.Events.AUDIO_TRACK_SWITCHING, this.#reflectAudioSelection);
+			engine?.off(Hls.Events.AUDIO_TRACK_SWITCHED, this.#reflectAudioSelection);
 			engine?.off(Hls.Events.LEVELS_UPDATED, this.#onLevelsUpdated);
 			engine?.off(Hls.Events.LEVEL_SWITCHED, this.#onLevelSwitched);
 			engine?.off(Hls.Events.DESTROYING, this.#teardown);
diff --git a/dist/dev/dom/store/features/remote-playback.js b/dist/dev/dom/store/features/remote-playback.js
index 9a00dbf2858199afe8a66c8f3796ba78efdfc7bc..16737d05303a2806575ba6ea0984d84bb8266af7 100644
--- a/dist/dev/dom/store/features/remote-playback.js
+++ b/dist/dev/dom/store/features/remote-playback.js
@@ -1,7 +1,9 @@
 import { isMediaRemotePlaybackCapable } from "../../../media/predicate.js";
 import { definePlayerFeature } from "../../feature.js";
-import { isRemotePlaybackConnected, requestRemotePlayback } from "../../presentation/remote-playback.js";
+import { isRemotePlaybackConnected, isRemotePlaybackConnecting, requestRemotePlayback } from "../../presentation/remote-playback.js";
 import { exitFullscreen, isFullscreen } from "../../presentation/fullscreen.js";
+import { castConnectionState, castDevicesAvailable, endCastSession, getCastContext, requiresCastFramework } from "../../media/google-cast/utils.js";
+import { castFramework, ensureCastFramework } from "../../media/google-cast/registry.js";
 import { isWebKitAirPlayCapable, listen } from "@videojs/utils/dom";
 //#region src/dom/store/features/remote-playback.ts
 const remotePlaybackFeature = definePlayerFeature({
@@ -11,15 +13,21 @@ const remotePlaybackFeature = definePlayerFeature({
 		remotePlaybackAvailability: "unsupported",
 		async toggleRemotePlayback() {
 			const { media, container } = target();
-			if (isRemotePlaybackConnected(media)) return requestRemotePlayback(media);
+			const capable = Boolean(isMediaRemotePlaybackCapable(media));
+			const connected = isRemotePlaybackConnected(media) || isRemotePlaybackConnecting(media) || castConnectionState() !== "disconnected";
+			if (connected) {
+				if (capable) return requestRemotePlayback(media);
+				endCastSession();
+				return;
+			}
+			if (!capable) return;
 			if (isFullscreen(container, media)) await exitFullscreen(media);
 			return await requestRemotePlayback(media);
 		}
 	}),
 	attach({ target, signal, set }) {
 		const { media } = target;
-		if (!isMediaRemotePlaybackCapable(media)) return;
-		if (isWebKitAirPlayCapable(media)) {
+		if (media && isWebKitAirPlayCapable(media)) {
 			const syncConnection = () => {
 				set({ remotePlaybackState: media.webkitCurrentPlaybackTargetIsWireless ? "connected" : "disconnected" });
 			};
@@ -32,6 +40,29 @@ const remotePlaybackFeature = definePlayerFeature({
 			syncConnection();
 			return;
 		}
+		if (requiresCastFramework()) {
+			const sync = () => {
+				set({
+					remotePlaybackState: castConnectionState(),
+					remotePlaybackAvailability: castDevicesAvailable() ? "available" : "unavailable"
+				});
+			};
+			ensureCastFramework().then(() => {
+				if (signal.aborted) return;
+				const ctx = getCastContext();
+				if (!ctx || !castFramework) return;
+				const { CAST_STATE_CHANGED, SESSION_STATE_CHANGED } = castFramework.CastContextEventType;
+				ctx.addEventListener(CAST_STATE_CHANGED, sync);
+				ctx.addEventListener(SESSION_STATE_CHANGED, sync);
+				signal.addEventListener("abort", () => {
+					ctx.removeEventListener(CAST_STATE_CHANGED, sync);
+					ctx.removeEventListener(SESSION_STATE_CHANGED, sync);
+				});
+				sync();
+			}, () => set({ remotePlaybackAvailability: "unsupported" }));
+			return;
+		}
+		if (!isMediaRemotePlaybackCapable(media)) return;
 		const syncState = () => set({ remotePlaybackState: media.remote.state });
 		syncState();
 		listen(media.remote, "connect", syncState, { signal });
diff --git a/dist/dev/dom/store/features/quality.js b/dist/dev/dom/store/features/quality.js
index c91df9b..9ce8132 100644
--- a/dist/dev/dom/store/features/quality.js
+++ b/dist/dev/dom/store/features/quality.js
@@ -39,6 +39,7 @@ const qualityFeature = definePlayerFeature({
 	}),
 	attach({ target, signal, set }) {
 		const { media } = target;
+		if (!media) return;
 		let videoRenditions = null;
 		let cleanup = null;
 		const getVideoRenditions = () => isMediaVideoRenditionCapable(media) ? media.videoRenditions : null;
diff --git a/dist/dev/dom/store/features/audio-track.js b/dist/dev/dom/store/features/audio-track.js
index f70ac68..53da741 100644
--- a/dist/dev/dom/store/features/audio-track.js
+++ b/dist/dev/dom/store/features/audio-track.js
@@ -29,6 +29,7 @@ const audioTrackFeature = definePlayerFeature({
 	}),
 	attach({ target, signal, set }) {
 		const { media } = target;
+		if (!media) return;
 		let audioTracks = null;
 		let cleanup = null;
 		const getAudioTracks = () => isMediaAudioTrackCapable(media) ? media.audioTracks : null;
diff --git a/dist/dev/dom/store/features/fullscreen.js b/dist/dev/dom/store/features/fullscreen.js
index ef8c703..749de28 100644
--- a/dist/dev/dom/store/features/fullscreen.js
+++ b/dist/dev/dom/store/features/fullscreen.js
@@ -27,6 +27,7 @@ const fullscreenFeature = definePlayerFeature({
 	attach({ target, signal, set }) {
 		const { media, container } = target;
 		set({ fullscreenAvailability: isFullscreenEnabled() ? "available" : "unsupported" });
+		if (!media) return;
 		const sync = () => set({ fullscreen: isFullscreen(container, media) });
 		sync();
 		listen(document, "fullscreenchange", sync, { signal });
diff --git a/dist/dev/dom/store/features/pip.js b/dist/dev/dom/store/features/pip.js
index 4620e78..87d0828 100644
--- a/dist/dev/dom/store/features/pip.js
+++ b/dist/dev/dom/store/features/pip.js
@@ -27,6 +27,7 @@ const pipFeature = definePlayerFeature({
 	attach({ target, signal, set }) {
 		const { media } = target;
 		set({ pipAvailability: isPictureInPictureEnabled() ? "available" : "unsupported" });
+		if (!media) return;
 		const sync = () => set({ pip: isPictureInPicture(media) });
 		sync();
 		listen(media, "enterpictureinpicture", sync, { signal });
diff --git a/dist/default/dom/store/features/quality.js b/dist/default/dom/store/features/quality.js
index c91df9b..9ce8132 100644
--- a/dist/default/dom/store/features/quality.js
+++ b/dist/default/dom/store/features/quality.js
@@ -39,6 +39,7 @@ const qualityFeature = definePlayerFeature({
 	}),
 	attach({ target, signal, set }) {
 		const { media } = target;
+		if (!media) return;
 		let videoRenditions = null;
 		let cleanup = null;
 		const getVideoRenditions = () => isMediaVideoRenditionCapable(media) ? media.videoRenditions : null;
diff --git a/dist/default/dom/store/features/audio-track.js b/dist/default/dom/store/features/audio-track.js
index f70ac68..53da741 100644
--- a/dist/default/dom/store/features/audio-track.js
+++ b/dist/default/dom/store/features/audio-track.js
@@ -29,6 +29,7 @@ const audioTrackFeature = definePlayerFeature({
 	}),
 	attach({ target, signal, set }) {
 		const { media } = target;
+		if (!media) return;
 		let audioTracks = null;
 		let cleanup = null;
 		const getAudioTracks = () => isMediaAudioTrackCapable(media) ? media.audioTracks : null;
diff --git a/dist/default/dom/store/features/fullscreen.js b/dist/default/dom/store/features/fullscreen.js
index ef8c703..749de28 100644
--- a/dist/default/dom/store/features/fullscreen.js
+++ b/dist/default/dom/store/features/fullscreen.js
@@ -27,6 +27,7 @@ const fullscreenFeature = definePlayerFeature({
 	attach({ target, signal, set }) {
 		const { media, container } = target;
 		set({ fullscreenAvailability: isFullscreenEnabled() ? "available" : "unsupported" });
+		if (!media) return;
 		const sync = () => set({ fullscreen: isFullscreen(container, media) });
 		sync();
 		listen(document, "fullscreenchange", sync, { signal });
diff --git a/dist/default/dom/store/features/pip.js b/dist/default/dom/store/features/pip.js
index 4620e78..87d0828 100644
--- a/dist/default/dom/store/features/pip.js
+++ b/dist/default/dom/store/features/pip.js
@@ -27,6 +27,7 @@ const pipFeature = definePlayerFeature({
 	attach({ target, signal, set }) {
 		const { media } = target;
 		set({ pipAvailability: isPictureInPictureEnabled() ? "available" : "unsupported" });
+		if (!media) return;
 		const sync = () => set({ pip: isPictureInPicture(media) });
 		sync();
 		listen(media, "enterpictureinpicture", sync, { signal });
